# TRACEai API — uses external PostgreSQL (e.g. Neon).
# Set DATABASE_URL in .env before running: docker compose up --build

services:
  api:
    build: .
    ports:
      - "8004:8004"
    env_file:
      - .env
    restart: unless-stopped
